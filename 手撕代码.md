# app.py 手撕代码详解（逐函数逐行级）

> 文件定位：[app.py](file:///d:/Pycharm%20Document/Use_LLM/LangChain_Edu_RAG/My_agent/app.py) — My_agent 的 **Streamlit 前端**（对话入口），职责：接收用户自然语言 → 意图识别 → 按 `conf.intent` 路由到 4 个 A2A Agent → 解析返回的多 part 结果（文本+图片）并渲染。本文逐函数解析其内部操作目的，并标注**每个被调函数的来源**。

---

## 〇、一句话总览

```
用户输入 → [intent_agent] 意图识别(JSON) → conf.intent[意图]→Agent名
→ 健康检查 check_tcp → A2AClient.send_task_async 发任务(120s超时)
→ 解析 Task.artifacts[0].parts → _extract_parts 拆 text/image → _render_assistant 渲染
→ 异常降级为统一友好话术，全程 trace_id 留痕
```

---

## 一、模块导入层（第 10-27 行）——逐条解析来源与用途

| 导入 | 来源 | 用途 |
|---|---|---|
| `import os / sys` | Python 标准库 | `sys.path.append(...)` 把项目父目录加入模块搜索路径（第 12 行），保证 `from My_agent.xxx` 能正确导入 |
| `import asyncio` | Python 标准库 | `asyncio.run(asyncio.wait_for(...))` 以同步方式执行异步 A2A 调用并加 120s 超时护栏（Streamlit 脚本是同步环境，无法直接 await） |
| `import uuid` | Python 标准库 | 第 149 行生成 8 位 `trace_id`（`uuid.uuid4().hex[:8]`），贯穿全链路日志与审计 |
| `import json / re / base64` | Python 标准库 | `json.loads` 解析意图 LLM 输出；`re.sub` 剥掉 LLM 可能加的 ```json 包裹；`base64.b64decode` 解码 A2A 返回的图片 bytes |
| `from datetime import datetime` + `import pytz` | 标准库 / pytz | 取 `Asia/Shanghai` 时区的当日日期喂给意图提示词，让 LLM 感知"今天"（如回答时间相关追问） |
| `import streamlit as st` | streamlit | 页面骨架、会话状态、聊天渲染 |
| `from python_a2a import AgentNetwork, A2AClient, Message, TextContent, MessageRole, Task` | 第三方包 **python-a2a==0.5.4** | 构建智能体网络（`AgentNetwork`）、A2A 客户端（`A2AClient`）、任务消息（`Message/TextContent/MessageRole/Task`）。这些类全部来自 pip 包 python_a2a（a2a 协议官方 Python SDK），源码不在项目内 |
| `from langchain_openai import ChatOpenAI` | langchain-openai | 构造 OpenAI 兼容 Chat 模型（deepseek 走 OpenAI 协议），temperature=0.1 求稳定、timeout=30、max_retries=2 |
| `from My_agent.config import Config` | **项目内 [config.py](file:///d:/Pycharm%20Document/Use_LLM/LangChain_Edu_RAG/My_agent/config.py)** | 全局配置：模型/数据库/Milvus/端口/**意图路由映射 `conf.intent`** |
| `from My_agent.create_logger import logger` | **项目内 [create_logger.py](file:///d:/Pycharm%20Document/Use_LLM/LangChain_Edu_RAG/My_agent/create_logger.py)** | 统一日志器（模块级 `logger = setup_logger('My_agent', Config().log_file)`，双写控制台 INFO + 文件 DEBUG） |
| `from My_agent.main_prompts import MyAgentPrompts` | **项目内 [main_prompts.py](file:///d:/Pycharm%20Document/Use_LLM/LangChain_Edu_RAG/My_agent/main_prompts.py)** | 意图识别提示模板 `intent_prompt()` 与功能说明 `HELP_TEXT` |
| `from My_agent.utils import health` | **项目内 [utils/health.py](file:///d:/Pycharm%20Document/Use_LLM/LangChain_Edu_RAG/My_agent/utils/health.py)** | 路由前 TCP 健康检查 `check_tcp` |

**设计意图**：所有依赖都在模块顶层一次性导入；`sys.path.append(父目录)` 是给"从项目根目录直接 `streamlit run My_agent/app.py`"这种运行方式兜底，保证 `My_agent.*` 包名可解析。

---

## 二、全局配置与页面设置（第 29-42 行）

```
29  conf = Config()          # 实例化配置对象（来源：config.py）
31  st.set_page_config(...)  # Streamlit 页面：标题/宽屏/图标
33-42 st.markdown("""<style>...""", unsafe_allow_html=True)  # 注入 CSS
```

逐段目的：
- `conf = Config()`：读取 deepseek 模型、意图→Agent 映射表 `conf.intent`、端口等。**注意**：意图路由完全由 `conf.intent` 字典驱动（`progress_query→ProgressQueryAgent` 等 4 项，来源 config.py 第 43-48 行），新增意图只需改配置。
- `st.set_page_config`：设置浏览器标签标题、宽屏 layout（便于左右分栏）、页面图标。
- CSS 注入：自定义 `.stChatMessage` 深蓝背景/圆角/阴影、白色文字，让聊天气泡美观；`unsafe_allow_html=True` 允许 HTML/CSS 注入。

---

## 三、会话状态初始化（第 44-67 行）

Streamlit 每次交互会**重新自上而下执行整个脚本**，因此用 `st.session_state` 在重跑间保留数据。`if "xxx" not in st.session_state` 保证只初始化一次。

| 状态项 | 初始化内容 | 目的 |
|---|---|---|
| `messages` | `[]` | 聊天气泡列表（含 user/assistant 消息与图片） |
| `agent_urls` | 4 个 Agent 的 base url（5020-5023） | 供健康检查与卡片展示用 |
| `agent_network` | `AgentNetwork(name="My_agent 签订进度智能助手网络")`，循环 `network.add(name, A2AClient(url, timeout=120))` | 注册 4 个 A2A 客户端。**timeout=120 是关键决策**：RAG 文档问答约 40s，python_a2a 默认 30s 客户端超时会误判失败（这是踩坑后调优） |
| `llm` | `ChatOpenAI(model=conf.model_name, api_key=conf.api_key, base_url=conf.base_url, temperature=0.1, timeout=30, max_retries=2)` | 意图识别专用 LLM（deepseek，OpenAI 兼容接口）。temperature=0.1 保证 JSON 输出稳定；timeout=30/max_retries=2 防卡死 |
| `conversation_history` | `""` | 多轮对话文本，每轮追加 `\nUser: xxx` / `\nAssistant: xxx` |

**来源标注**：`AgentNetwork`/`A2AClient` 来自 python_a2a；`ChatOpenAI` 来自 langchain_openai；`conf` 来自 config.py。

---

## 四、辅助函数一：`_extract_parts(parts)`（第 71-88 行）

```python
def _extract_parts(parts):
    """从 A2A artifact parts 提取 (text, images)；images 为 [{bytes, mime, name}]"""
    text = ""
    images = []
    for part in parts or []:
        if not isinstance(part, dict):
            continue
        if part.get("type") == "text":
            text += part.get("text", "")
        elif part.get("type") == "file":
            f = part.get("file") or {}
            if str(f.get("mimeType", "")).startswith("image/"):
                images.append({
                    "bytes": f.get("bytes", ""),
                    "mime": f.get("mimeType", "image/png"),
                    "name": f.get("name", "图片"),
                })
    return text, images
```

**目的**：A2A 规范中一个 `artifact` 由多个 `part` 组成，part 有两种类型——`text`（纯文本答案）和 `file`（文件载荷，可带 `mimeType/bytes/name`）。本函数把 A2A 返回的结构化 parts 拆成「文本 + 图片列表」，供前端分别渲染。这是实现"文档问答有图像返回"的关键解码环节。

逐行目的：
- `text="" / images=[]`：初始化两个返回容器。
- `for part in parts or []`：`parts or []` 兜底空值（服务端异常时 artifacts 可能为空）。`not isinstance(part, dict) → continue`：防御性跳过非 dict 的脏数据（不同版本的 python_a2a part 可能被序列化成 dict）。
- `part.get("type") == "text"`：累加文本片段到 `text`（文本答案）。
- `part.get("type") == "file"`：取 `part["file"]` 子对象（A2A FilePart 的载荷字段），若 `mimeType` 以 `image/` 开头，则视为图片，把 `bytes/mime/name` 打包进 images 列表。`str(...).startswith("image/")` 用字符串前缀判断而非精确匹配，兼容 `image/png`、`image/jpeg` 等任意图片类型。

**来源标注**：纯逻辑函数，无外部依赖；输入的 `parts` 结构来自 python_a2a 的 Task.artifacts（服务端组装，见 a2a_server/document_query_server.py 组装 file part 的方式）。

---

## 五、辅助函数二：`_render_assistant(text, images=None)`（第 91-97 行）

```python
def _render_assistant(text, images=None):
    st.markdown(text)
    for img in images or []:
        try:
            st.image(base64.b64decode(img["bytes"]), caption=img.get("name", ""))
        except Exception:
            continue
```

**目的**：把助手回复渲染到聊天区——先渲染文本，再逐张渲染图片。

逐行目的：
- `st.markdown(text)`：Streamlit 原生 markdown 渲染文本答案。
- `st.image(base64.b64decode(img["bytes"]), ...)`：**来源为 streamlit 的 `st.image`**；`base64.b64decode` 把 A2A 传输的 base64 字符串解码为原始字节喂给 st.image 显示；`caption` 显示图片名。
- `try/except Exception: continue`：单张图片解码失败（如空 bytes、损坏数据）直接跳过该张，**不影响整体回答渲染**——容错设计，图片是增强不是依赖。

---

## 六、核心函数：`intent_agent(user_input)`（第 100-118 行）

```python
def intent_agent(user_input):
    chain = MyAgentPrompts.intent_prompt() | st.session_state.llm
    current_date = datetime.now(pytz.timezone('Asia/Shanghai')).strftime('%Y-%m-%d')
    intent_response = chain.invoke({
        "conversation_history": '\n'.join(st.session_state.conversation_history.split("\n")[-6:]),
        "query": user_input,
        "current_date": current_date,
        "help_text": MyAgentPrompts.HELP_TEXT,
    }).content.strip()
    logger.info(f"意图识别原始响应: {intent_response}")
    intent_response = re.sub(r'^```json\s*|\s*```$', '', intent_response).strip()
    intent_output = json.loads(intent_response)
    intent = intent_output.get("intent", "")
    rewritten_query = intent_output.get("rewritten_query", "")
    follow_up = intent_output.get("follow_up", "")
    help_text = intent_output.get("help", "")
    logger.info(f"intent: {intent}||rewritten_query: {rewritten_query}||follow_up: {follow_up}||help: {help_text[:60]}")
    return intent, rewritten_query, follow_up, help_text
```

**目的**：一次 LLM 调用同时完成「意图分类 + 查询改写 + 追问文案 + 帮助文案」四件事，输出严格 JSON 供上层路由。

逐行目的：
- `MyAgentPrompts.intent_prompt()`：**来源为 main_prompts.py 的静态方法**，返回 `ChatPromptTemplate.from_template(...)`（LangChain 提示模板）。模板内容：系统角色（意图识别专家）+ 四类业务意图定义 + follow_up/help 兜底规则 + 严格 JSON 输出约束 + 8 条 few-shot 示例 + help_text + 当前日期 + 对话历史 + 用户 query 占位符。
- `| st.session_state.llm`：LangChain 的 **管道操作符（LCEL）**，把「提示模板 → 模型」串成一条 chain。`st.session_state.llm` 来源为会话状态里的 ChatOpenAI（langchain_openai）。
- `current_date`：取上海时区日期字符串，注入提示词，让模型感知当前日期。
- `chain.invoke({...})`：真正调用 LLM。四个注入变量：
  - `conversation_history`：**只取最近 6 行**历史（`split("\n")[-6:]`）——窗口截断控 token，避免无关旧历史干扰意图判断；
  - `query`：用户本轮原始输入；
  - `help_text`：来自 main_prompts.py 的 `MyAgentPrompts.HELP_TEXT` 常量（四类功能说明+示例问句）；
  - `.content.strip()`：取模型输出的文本并去首尾空白。
- `logger.info(...)`：记录原始响应，便于排查 LLM 格式漂移。
- `re.sub(r'^```json\s*|\s*```$', '', ...)`：**健壮性处理**——剥掉 LLM 偶尔会带的 ```json 代码块包裹（首尾各匹配一次）。
- `json.loads(...)`：解析为 dict。若失败会抛 `json.JSONDecodeError`，由主流程第 205 行捕获并降级为"意图识别解析失败，请重试"。
- 四个 `get(key, "")`：分别取 intent / rewritten_query / follow_up / help，**缺省给空串**兜底，避免 KeyError。
- `logger.info(...)`：结构化记录四个字段，便于评估意图准确率。
- `return` 四元组给主流程分发。

**调用来源汇总**：`MyAgentPrompts.intent_prompt()` / `MyAgentPrompts.HELP_TEXT` ← main_prompts.py；`st.session_state.llm` ← langchain_openai.ChatOpenAI；`datetime/pytz` ← 标准库/pytz；`logger` ← create_logger.py。

---

## 七、侧边栏（第 122-127 行）

```
with st.sidebar:
    st.header / st.markdown(MyAgentPrompts.HELP_TEXT)  # 功能说明
    st.markdown("**技术架构：** A2A 多智能体 + MCP 工具 + MySQL + Milvus(RAG) + DeepSeek")
    st.markdown("**文档问答：** RAG 知识检索（8023）已接入...")
```

**目的**：功能引导 + 技术栈说明。`HELP_TEXT` 来源为 main_prompts.py 常量，与 LLM 的 help 输出共用同一文案，保证"人看到的说明"和"模型输出的说明"口径一致。

---

## 八、主界面 col1：对话区与核心处理流程（第 133-218 行）——全文重点

```
133  col1, col2 = st.columns([2, 1])   # 左右分栏：对话区(宽) | Agent卡片(窄)
```

### 8.1 历史消息渲染（第 137-142 行）

```python
for message in st.session_state.messages:
    with st.chat_message(message["role"]):
        if message["role"] == "assistant":
            _render_assistant(message.get("content", ""), message.get("images"))
        else:
            st.markdown(message["content"])
```

- `st.chat_message(role)`：Streamlit 原生聊天消息容器（user 右侧/assistant 左侧气泡）。
- assistant 消息走 `_render_assistant`（文本+图片），user 消息直接 `st.markdown`。保证刷新页面后历史完整重现。

### 8.2 用户输入与 trace 记录（第 144-150 行）

```python
if prompt := st.chat_input("请输入您的问题，例如：查询西湖文旅的签订进度..."):
    with st.chat_message("user"):
        st.markdown(prompt)
    st.session_state.messages.append({"role": "user", "content": prompt})
    st.session_state.conversation_history += f"\nUser: {prompt}"
    trace_id = uuid.uuid4().hex[:8]
    logger.info(f"[trace:{trace_id}] 用户输入: {prompt}")
```

- `st.chat_input(...)`：聊天输入框，`:=` 海象运算符保证只在有输入时进入分支。
- 把用户消息写入 `messages` 与 `conversation_history`。
- **生成 trace_id**（`uuid.uuid4().hex[:8]`，来源标准库 uuid）：本次请求的唯一链路标识，后续拼进发给 Agent 的文本、写进日志，最终状态修改链路会把它落进 audit_log。

### 8.3 状态容器与异常兜底框架（第 152-153、205-218 行）

```python
with st.status("正在处理您的请求...", expanded=False) as status:   # 折叠的状态卡片
    try:
        ...
    except json.JSONDecodeError as json_err:      # 意图识别 JSON 解析失败
        ...
    except Exception as e:                        # 其余一切异常
        ...
```

- `st.status(...)`：Streamlit 状态容器，可动态 `status.update(label=...)` 展示处理阶段（识别意图→调用 XX Agent→完成），失败时显示"解析失败/处理失败"。
- **双层异常兜底**：`JSONDecodeError` 单独捕获（最可能的失败点，给专门话术"意图识别解析失败，请重试"）；`Exception` 兜底其余错误，统一话术"处理失败，请稍后重试或联系相关负责人处理"，**绝不把堆栈抛给用户**。两处都 `logger.error(f"[trace:{trace_id}] ...")` 留痕。

### 8.4 意图分发主干（第 155-200 行）

```python
intent, rewritten_query, follow_up, help_text = intent_agent(prompt)
resp_images = []
```

- 调意图识别（第六节），初始化图片容器。

**分支一：help（闲聊/超出服务范围）**（第 158-161 行）
```python
if intent == "help":
    response = help_text if help_text else MyAgentPrompts.HELP_TEXT
```
- 优先用 LLM 生成的 help，空则回退 `HELP_TEXT` 常量（main_prompts.py），双保险。并把 response 追加进历史。

**分支二：follow_up（缺关键信息）**（第 162-165 行）
```python
elif intent == "follow_up":
    response = follow_up if follow_up else "请提供更明确的信息，例如：查询西湖文旅的签订进度。"
```
- 展示 LLM 生成的追问文案，空则回退写死话术。**前置追问**设计：信息不足不进业务 Agent，省算力且避免错误查询。

**分支三：正常业务路由**（第 166-200 行）
```python
agent_name = conf.intent.get(intent)      # 来源 config.py：意图→Agent名映射
if not agent_name:
    response = "暂不支持该操作。您可以输入「查询西湖文旅的进度」或「查一下张伟的资料」。"
else:
    query_str = rewritten_query if rewritten_query else prompt   # 改写优先
    # ① 健康检查
    _p = urlparse(agent_url)              # 来源 urllib.parse（第176行局部导入）
    if not health.check_tcp(_p.hostname, _p.port, timeout=2):    # 来源 utils/health.py
        response = f"【{agent_name}】服务暂时不可用，请稍后重试或联系相关负责人处理。"
    else:
        agent = st.session_state.agent_network.get_agent(agent_name)   # 来源 python_a2a
        chat_history = (f"[trace:{trace_id}] " +
                        '\n'.join(st.session_state.conversation_history.split("\n")[-7:-1]) +
                        f'\nUser: {query_str}')
        message = Message(content=TextContent(text=chat_history), role=MessageRole.USER)  # python_a2a
        task = Task(id="task-" + str(uuid.uuid4()), message=message.to_dict())            # python_a2a
        raw_response = asyncio.run(asyncio.wait_for(agent.send_task_async(task), timeout=120))  # python_a2a + 标准库
        # 结果解析
        if raw_response.status.state == 'completed':
            response, resp_images = _extract_parts(raw_response.artifacts[0]['parts'])
        else:
            response = raw_response.status.message['content']['text']
```

逐段目的：
- **路由表**：`conf.intent.get(intent)` 返回 Agent 名（config.py 第 43-48 行），查不到说明 LLM 输出了未定义意图，给"暂不支持"话术。
- **改写优先**：`rewritten_query if rewritten_query else prompt`——用改写后更明确的问题作为真正下发给 Agent 的 query。
- **健康检查前置**：`health.check_tcp(host, port, timeout=2)`（来源 utils/health.py：`socket.create_connection` 2 秒探测）——Agent 不可达时**不发起 A2A 调用**，直接降级为"服务暂时不可用"，避免用户干等超时。
- **构造 A2A 任务**：
  - `chat_history`：把 `[trace:xxx]` + 最近 7 行历史（`[-7:-1]`，注意**这里不包含刚追加的当前行**，因为当前 query 在最后一行手动拼）+ `User: query_str` 拼成发给 Agent 的对话文本。**来源均为项目自有逻辑**，Message/TextContent/MessageRole/Task 类来自 python_a2a。
  - `Message(content=TextContent(text=chat_history), role=MessageRole.USER)`：A2A 规范的消息结构（内容+角色）。
  - `Task(id="task-"+uuid, message=message.to_dict())`：构造任务对象，`to_dict()` 转为协议 JSON。
- **发送与超时**：`agent.send_task_async(task)`（来源 python_a2a 的 A2AClient 异步方法）用 `asyncio.wait_for(..., timeout=120)` 包一层 120s 硬超时，再用 `asyncio.run` 在同步的 Streamlit 脚本里执行。**120s 是为 RAG 文档问答（约 40s）留的裕量**，比 python_a2a 默认 30s 宽裕得多。
- **超时降级**：`except asyncio.TimeoutError` → "响应超时，请稍后重试或联系相关负责人处理"。
- **结果解析**：
  - `raw_response.status.state == 'completed'`（来源 python_a2a 的 TaskStatus/TaskState）：成功则取 `artifacts[0]['parts']` 交给 `_extract_parts` 拆文本与图片（对应第六/四节）；
  - 非 completed（input_required/failed）：取 `status.message['content']['text']` 直接展示（如追问文案/失败原因），**A2A 协议把"追问/失败"变成一等状态，前端无需额外解析**。

### 8.5 结果渲染与历史回写（第 199-204 行）

```python
status.update(label="完成")
st.session_state.conversation_history += f"\nAssistant: {response}"
with st.chat_message("assistant"):
    _render_assistant(response, resp_images)
st.session_state.messages.append({"role": "assistant", "content": response, "images": resp_images})
```

- 更新状态卡片为"完成"；把助手回复追加进对话历史（供下一轮多轮上下文）；渲染文本+图片；持久化到 messages 供下次重跑渲染。

---

## 九、主界面 col2：Agent 卡片区（第 220-231 行）

```python
for agent_name in st.session_state.agent_network.agents.keys():   # 来源 python_a2a AgentNetwork
    agent_card = st.session_state.agent_network.get_agent_card(agent_name)   # python_a2a
    with st.expander(f"Agent: {agent_name}", expanded=False):
        st.markdown(f"**技能**\n\n{agent_card.skills}")
        st.markdown(f"**描述**\n\n{agent_card.description}")
        st.markdown(f"**地址**\n\n{agent_url}")
        st.markdown("**状态**\n\n在线")
```

- 遍历已注册 Agent，用 `get_agent_card`（来源 python_a2a，拉取 AgentCard 能力发现信息）展示每个 Agent 的技能/描述/地址/状态。**演示价值**：面试/汇报时直接把 A2A 的能力发现机制可视化。
- 第 231 行是静态说明 8023 RAG 能力已接入。

---

## 十、页脚（第 233-234 行）

`st.markdown('<div class="footer">My_agent | 基于 A2A + MCP ... v1.0</div>', unsafe_allow_html=True)`

---

## 十一、调用来源总表（面试背诵版）

| 代码 | 来源 | 一句话作用 |
|---|---|---|
| `Config()` / `conf.intent` / `conf.model_name` 等 | 项目内 config.py | 全局配置与**意图→Agent 路由表** |
| `logger` | 项目内 create_logger.py | 控制台+文件双写日志 |
| `MyAgentPrompts.intent_prompt()` / `HELP_TEXT` | 项目内 main_prompts.py | 意图识别提示模板 / 功能帮助文案 |
| `health.check_tcp` | 项目内 utils/health.py | Agent 端口 TCP 探测（降级依据） |
| `AgentNetwork` / `A2AClient` / `get_agent` / `get_agent_card` / `send_task_async` / `Message` / `TextContent` / `MessageRole` / `Task` / `raw_response.status` | pip 包 **python-a2a==0.5.4** | A2A 协议客户端全套 |
| `ChatOpenAI` / chain `\|` 管道 / `.invoke()` | pip 包 **langchain-openai** / langchain-core | LLM 调用与 LCEL 链式组装 |
| `st.*` | pip 包 **streamlit** | 页面/会话状态/聊天/图片渲染 |
| `uuid / json / re / base64 / asyncio / datetime` + `pytz` | Python 标准库 / pytz | trace_id、JSON 兜底、图片解码、超时护栏、日期 |

---

## 十二、端到端时序（用户输入 → 展示）

```
用户输入
  │  st.chat_input 捕获
  ▼
记录 User 消息 + conversation_history；生成 trace_id
  ▼
intent_agent(prompt)           ──► 调 deepseek（意图 JSON：intent/rewritten_query/follow_up/help）
  │
  ├─ help ───────────────► 展示帮助文案
  ├─ follow_up ──────────► 展示追问文案
  └─ 业务意图(4类) ───────► conf.intent → Agent名
        │  check_tcp 探活 ──失败──► "服务暂时不可用"（不发起 A2A）
        ▼ 成功
  构造 A2A Message/Task（含 trace + 最近7行历史 + 改写后 query）
  asyncio.wait_for(send_task_async, 120s)
        │
        ├─ completed ──► _extract_parts(artifacts[0].parts) ──► text + images
        │                    └─ _render_assistant：markdown 文本 + base64 解码图片
        ├─ 其他状态 ──► 取 status.message 文本直接展示（追问/失败话术）
        └─ TimeoutError ──► "响应超时"降级
  ▼
回写 Assistant 历史 + 追加 messages；status 置"完成"
```

---

## 十三、关键设计点（面试加分项）

1. **一次 LLM 调用完成四件事**（分类/改写/追问/帮助）：意图识别不只是分类，而是结构化输出——少一次调用、少一次失败点。
2. **健康检查前置降级**：`check_tcp` 2 秒探测，Agent 挂了绝不发起 A2A，配合 start_all 的进程守护 3 秒自愈，用户无感知。
3. **120s 超时护栏**：`asyncio.wait_for` 包 python_a2a 的异步调用，解决 RAG 慢响应被默认 30s 误杀的问题。
4. **A2A 状态机利用**：completed / input_required / failed 是协议一等状态，前端只读状态字段即可区分"答案/追问/失败"，无需文本约定。
5. **多 part 多模态**：`_extract_parts` 把 text+file(图片) 拆开渲染，实现文档问答"带图返回"。
6. **双层异常兜底**：JSONDecodeError 与通用 Exception 分离处理，堆栈永不泄漏给用户，全程 trace_id 留痕可追溯。
